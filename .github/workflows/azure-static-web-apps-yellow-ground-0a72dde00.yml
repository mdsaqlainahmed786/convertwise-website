# DISABLED — wrong app_location, not connected to nimitai.com
# Kept for reference only. To re-enable, restore the 'on:' trigger.
name: Azure Static Web Apps CI/CD (yellow-ground - disabled)

on:
  workflow_dispatch:

jobs:
  build_and_deploy_job:
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
      - name: Build And Deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_YELLOW_GROUND_0A72DDE00 }}
          action: "upload"
          app_location: "/"
          output_location: "build"
